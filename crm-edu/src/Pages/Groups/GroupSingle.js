import React from "react";
import { useParams } from "react-router-dom";

export default function GroupSingle() {
  const { id } = useParams();
  return (
    <div>
      <div></div>
    </div>
  );
}
